/*
Global gradle configuration. This will be applied to all sub-projects.
*/
/**
 * Global build script =============================================================================
 */
buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.11.+'
    }
}

/**
 * All projects global configuration ===============================================================
 */
allprojects {
    repositories {
        mavenCentral()
    }
}

/**
 * Library options =================================================================================
 */
ext.artifactName = "fragments"
ext.versionCode = 320
ext.versionName = "3.2"

/**
 * Android SDK options =============================================================================
 */
ext.compileSdkVersion = 19
ext.buildToolsVersion = "19.1.0"
ext.minSdkVersion = 7
ext.targetSdkVersion = 19

/**
 * Global dependencies =============================================================================
 */